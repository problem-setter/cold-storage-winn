import React from 'react';
import './Header.css';

const Header = ({ theme, onToggleTheme }) => {
  return (
    <header className="page-header">
      {/* Header content is visually invisible but maintains layout space */}
    </header>
  );
};

export default Header;
